# Catalog-level dependencies for the data-analysis skills.
# These are installed by data-analysis-init into the project .venv.
# Analysis-specific packages should be added to the project-level requirements.txt.

duckdb>=0.9
pandas>=2.0
matplotlib>=3.7
python-dotenv>=1.0
pydantic>=2.0
pyyaml>=6.0
requests>=2.31
